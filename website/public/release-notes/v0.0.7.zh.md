## ✨ 新增功能

**安全**

- **Tool Guard 工具守卫**：在工具执行前进行安全检查，自动识别参数中的危险操作（如 Shell 命令里的 `rm`、`mv`）。发现风险后会拦截执行，等待用户通过 `/approve` 放行；拒绝列表中的工具则始终禁止调用。控制台新增**安全**设置页面，可管理检测规则和审批流程 ([#563](https://github.com/agentscope-ai/Minions/pull/563))

**频道与通信**

- **Mattermost 频道**：全新 Mattermost 集成，支持私聊和线程对话，可收发文本、图片、文件、视频和音频，并提供打字状态提示和灵活的访问控制 ([#1071](https://github.com/agentscope-ai/Minions/pull/1071))
- **Matrix 频道**：基于 matrix-nio 实现的 Matrix 协议接入，支持文本、图片、视频、音频和文件收发 ([#969](https://github.com/agentscope-ai/Minions/pull/969))
- **@提及过滤**：群聊中仅当机器人被 @提及（Telegram 还支持斜杠命令）时才会响应，可通过 `require_mention` 参数分频道配置，适用于 Discord、钉钉、飞书和 Telegram ([#1049](https://github.com/agentscope-ai/Minions/pull/1049))
- **Telegram Markdown 渲染**：将 LLM 输出的 Markdown 自动转为 Telegram HTML 格式，支持加粗、斜体、代码块、链接、引用、剧透等，发送失败时自动退回纯文本 ([#850](https://github.com/agentscope-ai/Minions/pull/850))
- **飞书表情回应**：消息处理成功后，自动在最后一条飞书回复上添加"DONE"表情 ([#1158](https://github.com/agentscope-ai/Minions/pull/1158))
- **飞书富文本媒体**：支持提取飞书 post 富文本消息中的媒体文件（视频、音频等），自动下载并作为附件传递 ([#1159](https://github.com/agentscope-ai/Minions/pull/1159))
- **QQ 图片消息**：智能体输出中可使用 `[Image: url]` 标签发送图片，私聊和群聊均可使用 ([#1212](https://github.com/agentscope-ai/Minions/pull/1212))

**模型与 AI 功能**

- **模型调用重试**：LLM API 遇到限流、超时、连接中断等临时错误时自动重试并指数退避，可通过 `MINIONS_LLM_MAX_RETRIES`、`MINIONS_LLM_BACKOFF_BASE`、`MINIONS_LLM_BACKOFF_CAP` 调整行为 ([#1150](https://github.com/agentscope-ai/Minions/pull/1150))
- **LM Studio 支持**：新增 LM Studio 为内置模型提供商，提供配置界面和使用文档 ([#1134](https://github.com/agentscope-ai/Minions/pull/1134))
- **Token 用量统计**：跨所有提供商的 Token 用量追踪，新增用量统计页面、查询 API 和 `get_token_usage` 工具 ([#775](https://github.com/agentscope-ai/Minions/pull/775), [#1349](https://github.com/agentscope-ai/Minions/pull/1349), [#1284](https://github.com/agentscope-ai/Minions/pull/1284))
- **提供商高级配置**：新增 `generate_kwargs` 编辑器，可自定义生成参数（如 temperature、top_p、enable_thinking）；无需 API 密钥的提供商（Ollama、LM Studio）在有可用模型时正确显示为已配置 ([#1170](https://github.com/agentscope-ai/Minions/pull/1170))

**控制台与界面**

- **工作区文件拖拽排序**：可拖拽调整已启用的系统提示词文件顺序，决定提示词的优先级 ([#1121](https://github.com/agentscope-ai/Minions/pull/1121))
- **聊天中切换模型**：聊天页面新增模型选择下拉框，对话过程中可随时切换模型 ([#1140](https://github.com/agentscope-ai/Minions/pull/1140))
- **智能体语言切换**：在控制台的智能体配置页面可直接更改智能体使用的语言 ([#1251](https://github.com/agentscope-ai/Minions/pull/1251))
- **工具批量开关**：工具页面新增"全部启用"和"全部禁用"按钮，方便批量管理 ([#1112](https://github.com/agentscope-ai/Minions/pull/1112))
- **聊天 URL 直达**：支持通过 `/chat/:chatId` 直接打开指定聊天会话，URL 随会话自动同步 ([#1221](https://github.com/agentscope-ai/Minions/pull/1221))
- **上下文管理配置**：智能体配置中新增上下文管理区域，可调节压缩比、保留比和工具结果压缩策略，并显示对应的 Token 阈值 ([#1247](https://github.com/agentscope-ai/Minions/pull/1247))
- **切换页面保留聊天**：导航到其他页面时聊天组件保持挂载，返回后输入内容、光标位置和运行状态均保留 ([#1340](https://github.com/agentscope-ai/Minions/pull/1340))

**技能**

- **AI 技能优化**：技能编辑器新增"AI 优化"按钮，使用当前模型以流式方式重写技能内容，支持中英俄三种语言的提示词 ([#853](https://github.com/agentscope-ai/Minions/pull/853))
- **技能卡片描述展示**：技能卡片直接展示 SKILL.md 中的 `description` 字段，无需打开即可了解技能用途 ([#1235](https://github.com/agentscope-ai/Minions/pull/1235))

**安装与平台**

- **自动选择 PyPI 镜像**：安装脚本会自动检测 pypi.org 连通性，不可达时自动切换至阿里云镜像 ([#925](https://github.com/agentscope-ai/Minions/pull/925), [#1330](https://github.com/agentscope-ai/Minions/pull/1330))
- **Docker 密钥持久化**：Docker 镜像新增 `/app/working.secret` 目录，用于持久保存提供商配置和 API 密钥 ([#1050](https://github.com/agentscope-ai/Minions/pull/1050))

## 🔄 优化改进

**模型与提供商**

- **连接测试信息增强**：连接测试失败时返回具体的错误描述，方便快速定位配置问题 ([#1120](https://github.com/agentscope-ai/Minions/pull/1120))
- **ModelScope 默认模型更新**：默认模型调整为 `Qwen3.5-122B-A10B` 和 `GLM-5` ([#1154](https://github.com/agentscope-ai/Minions/pull/1154))
- **提供商 ID 冲突自动解决**：添加重复 ID 的提供商时自动重命名（如 `openai` → `openai-custom`），不再报错；自定义提供商跳过连接测试 ([#1234](https://github.com/agentscope-ai/Minions/pull/1234))
- **模型发现改为手动触发**：进入提供商页面不再自动拉取模型列表，通过"发现模型"按钮手动刷新 ([#1342](https://github.com/agentscope-ai/Minions/pull/1342))

**性能优化**

- **工作区压缩异步化**：工作区导入导出的 zip 操作改为异步执行，不再阻塞主线程 ([#1128](https://github.com/agentscope-ai/Minions/pull/1128))
- **会话读写异步化**：会话的加载改为异步方法，避免磁盘操作影响服务响应 ([#1236](https://github.com/agentscope-ai/Minions/pull/1236))

**控制台、技能与系统**

- **工具页面视觉优化**：优化工具卡片的悬停效果、启用/禁用样式和状态标识 ([#1092](https://github.com/agentscope-ai/Minions/pull/1092))
- **容器环境检测优化**：简化容器运行环境的判断逻辑，提高环境变量处理的可靠性 ([#1075](https://github.com/agentscope-ai/Minions/pull/1075))
- **内置技能完善**：更新 docx、pdf、pptx、xlsx 技能的使用说明，新增 pdf/表单和 pptx/编辑专项文档；文件操作新增 `~` 主目录支持，Shell 子进程自动使用 venv 中的 Python ([#1147](https://github.com/agentscope-ai/Minions/pull/1147))
- **Himalaya 邮件技能**：新增通过 `template write | template send` 撰写和发送邮件的能力，补充了附件处理说明和 163 邮箱配置指南 ([#1279](https://github.com/agentscope-ai/Minions/pull/1279))
- **记忆文档重构**：重新组织了记忆和上下文相关文档的结构，同步更新 README 链接，`reme-ai` 升级至 0.3.0.6b3 ([#1190](https://github.com/agentscope-ai/Minions/pull/1190))
- **模型选择器样式修复**：修正聊天页头部模型选择器的对齐方式，视觉更统一 ([#1309](https://github.com/agentscope-ai/Minions/pull/1309))
- **配置与安全页面重构**：重新组织了智能体配置页和安全设置页的布局与样式，提升清晰度和响应式体验 ([#1333](https://github.com/agentscope-ai/Minions/pull/1333))

## 🐛 问题修复

**频道修复**

- **钉钉认证失败处理**：钉钉频道在认证失败时能够正确取消任务并干净退出，不再依赖固定时长的等待 ([#1039](https://github.com/agentscope-ai/Minions/pull/1039))
- **Discord 长消息分割**：超出 Discord 2000 字符限制的消息会自动在换行处拆分发送，代码块格式不受影响 ([#1001](https://github.com/agentscope-ai/Minions/pull/1001))
 - **频道配置类型对齐**：Matrix、Mattermost 和 MQTT 频道配置现已正确类型化，通过 API 配置时能获得完整校验 ([#1289](https://github.com/agentscope-ai/Minions/pull/1289))（注：这些频道在后续版本中已被移除/精简，当前不再内置）

**平台兼容性**

- **Windows Shell 编码**：Shell 命令输出改用智能解码（优先 UTF-8，回退系统编码），解决了 Windows 上混合编码输出乱码的问题 ([#1103](https://github.com/agentscope-ai/Minions/pull/1103))
- **Windows 进程清理**：Shell 命令超时时会正确终止整个进程树，避免子进程残留导致挂起 ([#1260](https://github.com/agentscope-ai/Minions/pull/1260))
- **桌面应用 SSL 与 Windows 修复**：修复打包桌面应用的 SSL 证书加载问题，保留系统 PATH，新增 `MINIONS_LOG_LEVEL` 支持，解决了 Windows 上的日志文件锁定和子进程输出阻塞问题 ([#1252](https://github.com/agentscope-ai/Minions/pull/1252), [#1282](https://github.com/agentscope-ai/Minions/pull/1282))
- **桌面应用输入法与导航**：修复使用中文等输入法时 Enter 键误发消息的问题，外部链接改为在系统浏览器中打开，webview 中可正常选择文本 ([#1239](https://github.com/agentscope-ai/Minions/pull/1239))
- **安装脚本修复**：修复 install.sh 中 `info` 函数在定义前被调用导致的 `command not found` 错误 ([#1330](https://github.com/agentscope-ai/Minions/pull/1330))

**控制台与界面**

- **魔术命令会话保护**：修复魔术命令意外覆盖完整会话状态的问题，改为仅更新所需字段 ([#1168](https://github.com/agentscope-ai/Minions/pull/1168))
- **Ollama 弹窗渲染优化**：修复打开 Ollama 模型管理弹窗时引发的不必要重渲染 ([#1242](https://github.com/agentscope-ai/Minions/pull/1242))
- **聊天请求去重**：修复多个组件同时请求同一聊天会话时产生重复 API 调用的问题 ([#1307](https://github.com/agentscope-ai/Minions/pull/1307))

## 📚 文档

- **路线图与产品对比**：更新了网站和 README 中的路线图（EN/ZH/JA），新增记忆系统和大小模型协同规划；更新了 Minions 与 OpenClaw 的功能对比表 ([#1062](https://github.com/agentscope-ai/Minions/pull/1062), [#1110](https://github.com/agentscope-ai/Minions/pull/1110))
- **贡献指南**：修正了 CONTRIBUTING_zh.md 中的语法和措辞问题 ([#1119](https://github.com/agentscope-ai/Minions/pull/1119))

## 🔧 CI/CD

- **Docker 发布流程优化**：重构 Docker CI 流程，`.post` 版本正确更新 `latest` 标签，beta/alpha/rc/dev 等预发布版本不再误更新 ([#1069](https://github.com/agentscope-ai/Minions/pull/1069))
- **测试自动化**：新增 GitHub Actions 测试工作流，覆盖单元测试和集成测试，同时提供 `scripts/run_tests.py` 用于本地运行 ([#1152](https://github.com/agentscope-ai/Minions/pull/1152))
- **测试触发条件优化**：添加路径过滤，测试仅在 `src/`、`tests/`、`pyproject.toml`、`setup.py` 和工作流文件变更时触发 ([#1245](https://github.com/agentscope-ai/Minions/pull/1245))
