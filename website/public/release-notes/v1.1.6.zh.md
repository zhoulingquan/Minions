## ✨ 新增功能

**Agent 系统**

- **Windows 环境诊断**：`minions doctor` 新增 Windows 专项检测，包括长路径支持、PowerShell 语言模式和工作目录路径长度 ([#4032](https://github.com/agentscope-ai/Minions/pull/4032))
- **Agent 状态 API**：新增 `GET /agents/{agentId}/agent-status` 接口，返回 Agent 运行状态、任务数量和任务时间戳 ([#4107](https://github.com/agentscope-ai/Minions/pull/4107))
- **定时任务会话隔离**：定时任务新增 `share_session` 选项，关闭后每次调度运行将创建独立的会话上下文 ([#4117](https://github.com/agentscope-ai/Minions/pull/4117))
- **GPT Image 2 插件**：新增通过 OpenAI GPT Image 2 API 生成图片的工具插件 ([#3911](https://github.com/agentscope-ai/Minions/pull/3911))
- **配置驱动的 Agent 名称**：Agent 名称改为从配置文件读取，不再使用硬编码默认值 ([#4140](https://github.com/agentscope-ai/Minions/pull/4140))

**聊天与控制台**

- **Whisper 语音输入**：使用服务端 Whisper 转录替代浏览器原生 Web Speech API，语音输入更可靠、跨浏览器兼容性更好 ([#3574](https://github.com/agentscope-ai/Minions/pull/3574))
- **LLM 自动生成会话标题**：首条消息发送后，LLM 在后台自动生成会话标题，替代原有的截断文本。可在 Agent 配置中通过"自动生成会话标题"开关控制 ([#3829](https://github.com/agentscope-ai/Minions/pull/3829))
- **Token 用量趋势图**：Token 用量页面新增按模型和按 Token 类型的趋势折线图 ([#4080](https://github.com/agentscope-ai/Minions/pull/4080), [#4094](https://github.com/agentscope-ai/Minions/pull/4094))
- **技能批量启用/禁用**：技能批量操作工具栏新增启用和禁用按钮，支持逐项错误提示 ([#4091](https://github.com/agentscope-ai/Minions/pull/4091))
- **Mermaid 图表渲染**：Markdown 中标记为 `mermaid` 的代码块现可在聊天和文件预览中渲染为交互式图表 ([#4146](https://github.com/agentscope-ai/Minions/pull/4146))
- **巴西葡萄牙语 (pt-BR)**：控制台界面和后端语言设置全面支持 pt-BR ([#4009](https://github.com/agentscope-ai/Minions/pull/4009), [#4143](https://github.com/agentscope-ai/Minions/pull/4143))

**模型服务商（Providers）**

- **火山引擎服务商**：新增火山引擎作为内置 OpenAI 兼容服务商，包含标准端点和 Coding Plan 端点 ([#3994](https://github.com/agentscope-ai/Minions/pull/3994))
- **DashScope 区域选择**：DashScope 的 Base URL 支持通修改预设的区域端点 ([#4074](https://github.com/agentscope-ai/Minions/pull/4074))
- **阿里云 Token Plan 服务商**：新增阿里云 Token Plan 作为内置服务商 ([#4122](https://github.com/agentscope-ai/Minions/pull/4122))
- **Anthropic 默认 Max Tokens 提升**：Anthropic 兼容模型的默认 `max_tokens` 提升至 16,384 ([#4054](https://github.com/agentscope-ai/Minions/pull/4054))

**频道（Channels）**

- **飞书交互式审批卡片**：飞书中的工具调用审批请求现以交互式按钮卡片形式展示，支持审批/拒绝后原地更新状态，未订阅回调时显示文档引导链接 ([#3941](https://github.com/agentscope-ai/Minions/pull/3941), [#3982](https://github.com/agentscope-ai/Minions/pull/3982))
- **企业微信群聊会话共享**：新增 `share_session_in_group` 开关，控制群聊成员共享同一会话还是各自独立 ([#3948](https://github.com/agentscope-ai/Minions/pull/3948))
- **飞书发送者上下文**：飞书发送者昵称现已传入 Agent 环境上下文 ([#4098](https://github.com/agentscope-ai/Minions/pull/4098))
- **企业微信交互式审批卡片**：企业微信中的工具调用审批请求现以交互式模板卡片形式展示 ([#4112](https://github.com/agentscope-ai/Minions/pull/4112))

**安全**

- **静态文件路径遍历防护**：控制台静态文件服务拒绝绝对路径请求，防止路径遍历攻击 ([#3973](https://github.com/agentscope-ai/Minions/pull/3973))
- **规则级别自动拒绝**：安全规则现支持配置自动拒绝工具调用，无需等待人工审批 ([#4046](https://github.com/agentscope-ai/Minions/pull/4046))

**技能与 CLI**

- **技能测试命令**：新增 `minions skills test` 命令，用于验证技能内容并执行安全扫描 ([#3999](https://github.com/agentscope-ai/Minions/pull/3999))
- **技能安装/卸载命令**：新增 `minions skills install` 和 `minions skills uninstall` 命令，支持技能池级别和 Agent 工作空间级别的安装 ([#4053](https://github.com/agentscope-ai/Minions/pull/4053))

## ⚡ 性能优化

- **控制台渲染优化**：通过稳定审批轮询状态比较和聊天运行时配置缓存化，消除不必要的重复渲染 ([#4052](https://github.com/agentscope-ai/Minions/pull/4052), [#4110](https://github.com/agentscope-ai/Minions/pull/4110), [#4153](https://github.com/agentscope-ai/Minions/pull/4153))
- **聊天历史导航优化**：非方向键输入跳过历史消息查找，并缓存用户消息列表避免每次按键重复扫描 ([#4130](https://github.com/agentscope-ai/Minions/pull/4130))
- **二维码轮询清理**：认证完成或二维码过期后立即停止轮询并清除状态指示 ([#4148](https://github.com/agentscope-ai/Minions/pull/4148))

## 🐛 修复

**频道（Channels）**

- **企业微信流式占位保活**：通过定期刷新保持"思考中..."占位流活跃，防止企业微信中状态卡住 ([#3950](https://github.com/agentscope-ai/Minions/pull/3950))
- **企业微信重连竞态修复**：修复企业微信 WebSocket 中双重重连调度和跨事件循环断连问题 ([#3963](https://github.com/agentscope-ai/Minions/pull/3963))
- **Telegram 网络重试**：Telegram 遇到临时网络错误时采用指数退避策略自动重连，而非静默退出 ([#4039](https://github.com/agentscope-ai/Minions/pull/4039))
- **微信定时任务缓冲区刷新**：定时任务/主动发送绕过正常完成路径时，立即刷新微信合并缓冲区 ([#4106](https://github.com/agentscope-ai/Minions/pull/4106))
- **Markdown 表格分片保留**：消息分片时保留 Markdown 表格结构，每个分片重复表头和分隔行 ([#4119](https://github.com/agentscope-ai/Minions/pull/4119))

**Agent 系统**

- **外部 Agent 调用超时**：`delegate_external_agent` 新增 60 秒默认超时，防止外部 Agent 无响应时无限等待 ([#3928](https://github.com/agentscope-ai/Minions/pull/3928))
- **Agent 配置热重载**：Agent 配置文件变更现通过完整的 Agent 重载流程处理，确保任务优雅排空 ([#4064](https://github.com/agentscope-ai/Minions/pull/4064))
- **Agent 配置保存修复**：修复控制台 Agent 配置编辑器保存时丢失嵌套设置的问题 ([#4157](https://github.com/agentscope-ai/Minions/pull/4157))

**MCP**

- **MCP 执行超时修复**：使用 `sse_read_timeout` 作为 MCP 工具执行超时，替代原来过短的 HTTP 连接超时 ([#4058](https://github.com/agentscope-ai/Minions/pull/4058), [#4061](https://github.com/agentscope-ai/Minions/pull/4061))
- **MCP 客户端生命周期泄漏修复**：修复 `close()` 跳过停止后台重连任务的问题，避免遗留孤立子进程 ([#4152](https://github.com/agentscope-ai/Minions/pull/4152))

**控制台与界面**

- **SSE 代理字符安全**：序列化前对非法代理对字符进行清理，防止 SSE 流崩溃 ([#3553](https://github.com/agentscope-ai/Minions/pull/3553))
- **CodeMirror 自动换行**：修复工具调用输入/输出块中长行溢出不换行的问题 ([#3960](https://github.com/agentscope-ai/Minions/pull/3960))
- **默认 Agent 自定义名称**：所有界面正确显示默认 Agent 的自定义名称 ([#4073](https://github.com/agentscope-ai/Minions/pull/4073))
- **文件预览路径修复**：修复文件预览路径中多余的 URL 前缀剥离导致链接异常的问题 ([#4089](https://github.com/agentscope-ai/Minions/pull/4089))

**基础设施**

- **Docker 备份恢复修复**：在 Docker 卷挂载点上通过交换目录内容替代重命名方式正确恢复密钥文件 ([#3916](https://github.com/agentscope-ai/Minions/pull/3916))
- **音频块路径修复**：`file://` URL 音频块返回解析后的绝对路径，并清理冗余的文件处理代码 ([#4021](https://github.com/agentscope-ai/Minions/pull/4021), [#4048](https://github.com/agentscope-ai/Minions/pull/4048))
- **日志轮转**：全平台使用 `RotatingFileHandler` 进行日志轮转 ([#4076](https://github.com/agentscope-ai/Minions/pull/4076))
- **本地回环代理绕过**：CLI 健康检查访问回环地址时绕过代理环境变量，避免在企业代理环境下连接失败 ([#4092](https://github.com/agentscope-ai/Minions/pull/4092))
- **Conda 打包修复**：在执行 `conda-pack` 前恢复 conda 打包工具，防止打包流程异常 ([#4093](https://github.com/agentscope-ai/Minions/pull/4093))

## 📝 文档

- **WSL2 超时 FAQ**：新增在 WSL2 NAT 模式下处理 `APITimeoutError` 的 FAQ 条目 ([#4005](https://github.com/agentscope-ai/Minions/pull/4005))
- **文档更新**：更新网站文档以反映最新功能 ([#4013](https://github.com/agentscope-ai/Minions/pull/4013))

## 🧪 测试与 CI

- **控制台单元测试**：引入 Vitest 测试框架，覆盖聊天、API 层和共享组件的单元测试与组件测试 ([#3559](https://github.com/agentscope-ai/Minions/pull/3559), [#4121](https://github.com/agentscope-ai/Minions/pull/4121))
- **集成冒烟测试**：新增应用启动和设置/环境变量冒烟测试 ([#4081](https://github.com/agentscope-ai/Minions/pull/4081))
